<h1>Editing Book</h1>

<%= render 'form', book: @book %>

<%= link_to 'Show', @book %> |
<%= link_to 'Back', books_path %>
<p>
    <% if @book.image.attached? %>
      <%= image_tag @book.image %>
    <% end %>
</p>
